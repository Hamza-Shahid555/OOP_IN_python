Inheritance
Overview

This file demonstrates static methods and attempts to create a child class.

Key Concepts
@staticmethod belongs to the class, not an object.
Child classes inherit features from parent classes.
Note

The nested ToyotaCar(Car) definition inside Car is incorrect and would need restructuring.
